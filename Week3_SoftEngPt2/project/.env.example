# Copy this file to .env and adjust if your local Postgres is on a different port.
#
# Default matches docker-compose.yml (host port 5433 -> container 5432)
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5433/classicmodels
