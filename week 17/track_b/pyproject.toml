[project]
name = "track-b"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "rishavdevtiwari", email = "rishavtiwari.ai@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "evidently[llm]>=0.7.23",
    "mlflow>=3.16.1",
    "pydantic>=2.13.5",
]

[project.scripts]
track-b = "track_b:main"

[build-system]
requires = ["uv_build>=0.12.19,<0.13.0"]
build-backend = "uv_build"
